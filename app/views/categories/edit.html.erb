
<h2><strong>ジャンル編集画面</strong></h2><br>
<% if @category.errors.any? %>
   <h3><%= @category.errors.count %>errors prohibited this obj from being saved:</h3>
     <ul>
       <% @category.errors.full_messages.each do |message| %>
       <li><%= message %></li>
       <% end %>
     </ul>
<% end %>
  <%= form_for(@category) do |c| %>
    <div class="field row">
     	<div class="col-xs-1">
        <p><strong>ジャンル</strong></p>
      </div>
      <%= c.text_field :name, class: "col-xs-2" %>
      <div class="col-xs-3">
        <%= c.submit "保存", class:"btn btn-primary" %>
      </div>
      <div class="col-xs-3">
        <%= link_to "削除", category_path, method: :delete, class:"btn btn-danger" %>
      </div>
    </div>
  <% end %>